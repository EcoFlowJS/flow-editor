import { ItemDataType } from "rsuite/esm/MultiCascadeTree";
import * as ai from "react-icons/ai";
import * as bs from "react-icons/bs";
import * as bi from "react-icons/bi";
import * as ci from "react-icons/ci";
import * as cg from "react-icons/cg";
import * as di from "react-icons/di";
import * as fi from "react-icons/fi";
import * as fc from "react-icons/fc";
import * as fa from "react-icons/fa";
import * as fa6 from "react-icons/fa6";
import * as gi from "react-icons/gi";
import * as go from "react-icons/go";
import * as gr from "react-icons/gr";
import * as hi from "react-icons/hi";
import * as hi2 from "react-icons/hi2";
import * as im from "react-icons/im";
import * as lia from "react-icons/lia";
import * as io from "react-icons/io";
import * as io5 from "react-icons/io5";
import * as lu from "react-icons/lu";
import * as md from "react-icons/md";
import * as pi from "react-icons/pi";
import * as rx from "react-icons/rx";
import * as ri from "react-icons/ri";
import * as si from "react-icons/si";
import * as sl from "react-icons/sl";
import * as tb from "react-icons/tb";
import * as tfi from "react-icons/tfi";
import * as ti from "react-icons/ti";
import * as vsc from "react-icons/vsc";
import * as wi from "react-icons/wi";

const aiIcons: ItemDataType<string>[] = Object.keys(ai).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Ant Design Icons",
  };
});

const bsIcons: ItemDataType<string>[] = Object.keys(bs).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Bootstrap Icons",
  };
});

const biIcons: ItemDataType<string>[] = Object.keys(bi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "BoxIcons",
  };
});

const ciIcons: ItemDataType<string>[] = Object.keys(ci).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Circum Icons",
  };
});

const cgIcons: ItemDataType<string>[] = Object.keys(cg).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "css.gg",
  };
});

const diIcons: ItemDataType<string>[] = Object.keys(di).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Devicons",
  };
});

const fiIcons: ItemDataType<string>[] = Object.keys(fi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Feather",
  };
});

const fcIcons: ItemDataType<string>[] = Object.keys(fc).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Flat Color Icons",
  };
});

const faIcons: ItemDataType<string>[] = Object.keys(fa).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Font Awesome 5",
  };
});

const fa6Icons: ItemDataType<string>[] = Object.keys(fa6).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Font Awesome 6",
  };
});

const giIcons: ItemDataType<string>[] = Object.keys(gi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Game Icons",
  };
});

const goIcons: ItemDataType<string>[] = Object.keys(go).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Github Octicons icons",
  };
});

const grIcons: ItemDataType<string>[] = Object.keys(gr).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Grommet-Icons",
  };
});

const hiIcons: ItemDataType<string>[] = Object.keys(hi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Heroicons",
  };
});

const hi2Icons: ItemDataType<string>[] = Object.keys(hi2).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Heroicons 2",
  };
});

const imIcons: ItemDataType<string>[] = Object.keys(im).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "IcoMoon Free",
  };
});

const liaIcons: ItemDataType<string>[] = Object.keys(lia).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Icons8 Line Awesome",
  };
});

const ioIcons: ItemDataType<string>[] = Object.keys(io).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Ionicons 4",
  };
});

const io5Icons: ItemDataType<string>[] = Object.keys(io5).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Ionicons 5",
  };
});

const luIcons: ItemDataType<string>[] = Object.keys(lu).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Lucide",
  };
});

const mdIcons: ItemDataType<string>[] = Object.keys(md).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Material Design icons",
  };
});

const piIcons: ItemDataType<string>[] = Object.keys(pi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Phosphor Icons",
  };
});

const rxIcons: ItemDataType<string>[] = Object.keys(rx).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Radix Icons",
  };
});

const riIcons: ItemDataType<string>[] = Object.keys(ri).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Remix Icon",
  };
});

const siIcons: ItemDataType<string>[] = Object.keys(si).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Simple Icons",
  };
});

const slIcons: ItemDataType<string>[] = Object.keys(sl).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Simple Line Icons",
  };
});

const tbIcons: ItemDataType<string>[] = Object.keys(tb).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Tabler Icons",
  };
});

const tfiIcons: ItemDataType<string>[] = Object.keys(tfi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Themify Icons",
  };
});

const tiIcons: ItemDataType<string>[] = Object.keys(ti).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "Typicons",
  };
});

const vscIcons: ItemDataType<string>[] = Object.keys(vsc).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "VS Code Icons",
  };
});

const wiIcons: ItemDataType<string>[] = Object.keys(wi).map((key) => {
  return {
    value: key,
    label: key,
    iconGroup: "VS Code Icons",
  };
});

const iconItems: ItemDataType<string>[] = [
  ...aiIcons,
  ...bsIcons,
  ...biIcons,
  ...ciIcons,
  ...cgIcons,
  ...diIcons,
  ...fiIcons,
  ...fcIcons,
  ...faIcons,
  ...fa6Icons,
  ...giIcons,
  ...goIcons,
  ...grIcons,
  ...hiIcons,
  ...hiIcons,
  ...hi2Icons,
  ...imIcons,
  ...liaIcons,
  ...ioIcons,
  ...io5Icons,
  ...luIcons,
  ...mdIcons,
  ...piIcons,
  ...rxIcons,
  ...riIcons,
  ...siIcons,
  ...slIcons,
  ...tbIcons,
  ...tfiIcons,
  ...tiIcons,
  ...vscIcons,
  ...wiIcons,
];

export default iconItems;
